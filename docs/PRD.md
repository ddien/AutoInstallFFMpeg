# FFmpeg Auto Setup - PRD & Wireframe

## Product Overview

**Tên sản phẩm:** FFmpeg Auto Setup
**Mục tiêu:** Cài đặt FFmpeg tự động cho người dùng Windows phổ thông
**Định dạng:** File `.exe` độc lập (không cần Python)

---

## Problem Statement

- Người dùng tool video cần FFmpeg nhưng không biết cách cài đặt
- Khi di chuyển/đổi tên folder → mất PATH → lỗi "system cannot find file"
- Cần giải pháp 1-click, không yêu cầu kiến thức kỹ thuật

---

## Solution

App GUI đơn giản với PyQt6, build thành `.exe` bằng PyInstaller:
1. Kiểm tra FFmpeg đã cài chưa
2. Tìm file zip local hoặc tự tải
3. Giải nén vào vị trí cố định
4. Tự động thêm vào PATH
5. Xác nhận thành công

---

## Wireframe

```
┌─────────────────────────────────────────────────────────┐
│  FFmpeg Auto Setup                              [─][□][×]│
├─────────────────────────────────────────────────────────┤
│                                                         │
│     ┌─────────────────────────────────────────────┐     │
│     │                                             │     │
│     │            [FFmpeg Icon/Logo]               │     │
│     │                                             │     │
│     │         FFmpeg Auto Setup v1.0              │     │
│     │                                             │     │
│     └─────────────────────────────────────────────┘     │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │ Status Area (Progress Log)                        │  │
│  │                                                   │  │
│  │ [✓] Kiểm tra FFmpeg...                           │  │
│  │ [✓] Tìm thấy: ffmpeg.zip                         │  │
│  │ [◐] Đang giải nén... 45%                         │  │
│  │ [ ] Thêm vào PATH                                │  │
│  │                                                   │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│  ┌───────────────────────────────────────────────────┐  │
│  │ ████████████████████░░░░░░░░░░  65%               │  │
│  └───────────────────────────────────────────────────┘  │
│                                                         │
│           ┌─────────────────────────────┐               │
│           │     🚀 Cài đặt FFmpeg       │               │
│           └─────────────────────────────┘               │
│                                                         │
│  Thư mục cài đặt: C:\Tools\ffmpeg                      │
│                                                         │
└─────────────────────────────────────────────────────────┘
```

### Trạng thái UI

**1. Chưa cài đặt (Initial State)**
```
┌─────────────────────────────────────────┐
│ Status: FFmpeg chưa được cài đặt        │
│                                         │
│ [████████████████] 0%                   │
│                                         │
│        [ 🚀 Cài đặt FFmpeg ]            │
└─────────────────────────────────────────┘
```

**2. Đang cài đặt (Installing)**
```
┌─────────────────────────────────────────┐
│ [✓] Kiểm tra FFmpeg                     │
│ [✓] Tìm nguồn cài đặt                   │
│ [◐] Đang giải nén... 67%                │
│                                         │
│ [████████████░░░░] 67%                  │
│                                         │
│        [ ⏳ Đang xử lý... ]              │
└─────────────────────────────────────────┘
```

**3. Hoàn tất (Success)**
```
┌─────────────────────────────────────────┐
│ [✓] Kiểm tra FFmpeg                     │
│ [✓] Tìm nguồn cài đặt                   │
│ [✓] Giải nén hoàn tất                   │
│ [✓] Đã thêm vào PATH                    │
│                                         │
│ [████████████████] 100%                 │
│                                         │
│ ✅ Cài đặt thành công!                  │
│ Vui lòng khởi động lại CMD              │
│                                         │
│        [ ✓ Hoàn tất - Đóng ]            │
└─────────────────────────────────────────┘
```

**4. Đã cài sẵn (Already Installed)**
```
┌─────────────────────────────────────────┐
│ ✅ FFmpeg đã được cài đặt!              │
│                                         │
│ Version: ffmpeg 6.1.1                   │
│ Path: C:\Tools\ffmpeg\bin               │
│                                         │
│        [ Cài đặt lại ]  [ Đóng ]        │
└─────────────────────────────────────────┘
```

---

## Technical Specs

### Tech Stack
- **GUI Framework:** PyQt6
- **Build Tool:** PyInstaller (single .exe)
- **Target OS:** Windows 10/11 x64

### File Structure
```
AutoFixFFMpeg/
├── docs/
│   └── PRD.md              # This file
├── src/
│   ├── main.py             # Entry point
│   ├── ui/
│   │   └── main_window.py  # PyQt6 UI
│   └── core/
│       └── installer.py    # Install logic
├── assets/
│   └── icon.ico            # App icon
├── build.bat               # Build script
├── requirements.txt        # Dependencies
└── ffmpeg.zip              # Bundled FFmpeg (optional)
```

### Dependencies
```
PyQt6>=6.5.0
pyinstaller>=6.0.0
```

### Build Command
```bash
pyinstaller --onefile --windowed --icon=assets/icon.ico --name=FFmpegSetup src/main.py
```

---

## Implementation Plan

| Phase | Task | Priority |
|-------|------|----------|
| 1 | Tạo cấu trúc project | High |
| 2 | Implement core/installer.py (logic cũ) | High |
| 3 | Tạo UI với PyQt6 | High |
| 4 | Kết nối UI với logic | High |
| 5 | Test trên Windows | High |
| 6 | Build .exe với PyInstaller | High |
| 7 | Test .exe trên máy không có Python | Medium |

---

## Success Criteria

- [ ] User double-click .exe → cửa sổ hiện lên trong 2s
- [ ] Cài đặt hoàn tất trong < 60s (có sẵn zip)
- [ ] Cài đặt hoàn tất trong < 5 phút (tải từ internet)
- [ ] Sau cài đặt, mở CMD mới → `ffmpeg -version` chạy được
- [ ] File .exe < 50MB
